# 必需
requests>=2.31
pdfplumber>=0.11

# 仅当处理扫描件（图片型 PDF）需要 --ocr 时安装；
# 还需先安装系统包：
#   Ubuntu: sudo apt install tesseract-ocr tesseract-ocr-chi-sim poppler-utils
#   macOS : brew install tesseract tesseract-lang poppler
pytesseract>=0.3.10
pdf2image>=1.17
Pillow>=10
